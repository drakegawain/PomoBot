async def ch_session():
  #Check session status
  #if status is open or closed return false
  return

async def gather(dictionary:dict):
  #Return last session
  last_session=dictionary.len
  last_session=dictionary[last_session]
  return last_session